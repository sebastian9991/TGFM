[project]
name = "tgfm"
version = "0.1.0"
description = "Text-Graph Foundation Model."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pre-commit>=4.5.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
